#include <bits/stdc++.h>

using namespace std;

/* A + B 
You are given two integers A and B. Calculate their sum and output it without leading zeros.
Input: Two lines of input data contain integers A and B (1 ≤ A, B ≤ 105).
Output: Output A + B without leading zeros. 
*/

int main() {
    int a, b;
    cin >> a >> b; 
    cout << a + b << endl;
    return 0;
}